# =============================================================================
# 99_paths_local.R — LOCAL CONFIGURATION (do not commit)
# Copy this file to R/99_paths_local.R and edit paths for your machine.
# R/99_paths_local.R is gitignored and will never be pushed.
# =============================================================================

# Path to the triadic diffusion dataset (≈17.3M rows, ~2–4 GB .rds)
PATH_TRIADIC <- "dt_con_cs_nestedness.rds"

# Path to the multi-threshold dataset (for RCA sensitivity, script 07)
PATH_MULTITHR <- "datos_eventos_v13_MULTITHR/all_events_v13_multithr.rds"

# Output root (all generated tables, figures, rds go here)
OUTPUT_ROOT <- "output_SI_identity"
